# Importer Template

Use this folder as a starting point for future local-first OpenBrain importers.

Importer packages should include:

```text
README.md
metadata.json
sync-log.example.json
import-report.example.md
```

Keep real sync logs and import reports out of Git unless they are fully sanitized examples.
