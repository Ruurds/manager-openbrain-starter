[project]
name = "manager-openbrain-starter"
version = "0.1.0"
requires-python = ">=3.12"
description = "Local-first AI operating system scaffold for managers"

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
